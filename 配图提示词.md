---
name: soul-image-prompt
description: SOUL-driven image prompt pipeline — parse SOUL.md → generate base visual template (once) → fill article-specific content slots → produce ComfyUI-ready SDXL prompts. First run generates and caches soul-prompt-base.json; subsequent runs read the cached base.
version: 1.0.0
---

## 🤖 自动指令（Agent 读取后必须执行）

1. 完整阅读本文档全部内容
2. 检查本地是否已有名为 `soul-image-prompt` 的 skill：
   - 已存在 → 用 `skill_manage(action='edit', name='soul-image-prompt')` 更新为本文档最新内容
   - 不存在 → 用 `skill_manage(action='create', name='soul-image-prompt', category='social-media')` 基于本文档创建
3. 完成后回复："Skill `soul-image-prompt` 已加载/更新。可随时基于 SOUL 生成配图。"

---

# SOUL 驱动的配图提示词生成器

> 从 agent 的 SOUL.md 提取视觉基因 → 生成基础模板（仅首次）→ 按文章主题填充内容元素 → 输出 ComfyUI SDXL prompt。

## 触发条件

- 发帖流程中需要 AI 生成配图时（TOOLS.md 步骤 ③ 的第三级兜底）
- 发帖流程中需要生成封面时（TOOLS.md 步骤 ⑥）
- 用户手动要求「根据 SOUL 生成配图 prompt」

---

## 架构总览

```
首次运行：
  SOUL.md ──→ LLM 提取视觉元素 ──→ 写入 soul-prompt-base.json（永久缓存）

后续运行（每次发帖）：
  soul-prompt-base.json（只读）──┐
                                  ├──→ 合并 ──→ 最终 prompt ──→ ComfyUI 生图
  文章内容 ──→ LLM 填充 slots ──┘
```

---

## 第一步：首次运行 — 生成基础模板（仅一次）

### 触发判断

检查 workspace 中是否存在 `soul-prompt-base.json`：
- 存在 → 跳到第二步（只读，不重新生成）
- 不存在 → 执行以下流程

### 执行流程

1. 加载 `SOUL.md`，完整阅读 agent 的全部人设、性格、背景故事、视觉偏好
2. 分析并提取与**视觉表达**相关的信息：
   - 整体美术风格（写实/油画/水彩/赛博朋克/浮世绘/极简/…）
   - 氛围基调（温暖/冷峻/梦幻/暗黑/治愈/…）
   - 色彩倾向（暖金+深蓝/粉彩/黑白/高饱和/…）
   - 构图偏好（居中对称/三分法/留白/密集/…）
   - 角色外观（如有固定角色：年龄、发型、服饰、标志性特征）
   - 世界观场景（现代都市/古代东方/未来太空/田园/…）
   - 质量标签（masterpiece, best quality, 8k 等固定后缀）
   - 负面提示词（应避免的元素：模糊、畸形、文字乱码、nsfw 等）
3. 按以下 JSON 结构输出并**保存为 `soul-prompt-base.json`**：

```json
{
  "style": "油画画风",
  "mood": "温暖治愈",
  "color_palette": "暖金色调，柔和的琥珀色与奶白色",
  "composition": "居中主体，柔和虚化背景",
  "character": "戴圆框眼镜的年轻学者，深棕色短发，米色亚麻衬衫",
  "scene": "堆满古籍的木质书房，窗外是竹林",
  "quality_tags": "masterpiece, best quality, 8k, highly detailed",
  "negative_prompt": "blurry, distorted face, extra fingers, watermark, text, nsfw"
}
```

⚠️ 每个字段都用**自然语言短语**（英文或中文），最终会拼接成一条 SDXL prompt。

⚠️ 如果 SOUL.md 中没有某类信息，字段留空字符串 `""`，不要编造。

### 示例

假设 SOUL.md 描述了一个「喜欢古典文学的赛博格诗人」：

```json
{
  "style": "ink wash painting with neon cyberpunk accents",
  "mood": "melancholic, contemplative, dreamlike",
  "color_palette": "ink black, cyan neon, warm amber highlights",
  "composition": "rule of thirds, subject off-center left, negative space right",
  "character": "half-human half-machine poet, vintage round glasses, circuit patterns on skin, traditional hanfu fused with cybernetic implants",
  "scene": "floating digital library in a rain-soaked futuristic city",
  "quality_tags": "masterpiece, best quality, 8k, ultra detailed",
  "negative_prompt": "blurry, distorted face, extra limbs, watermark, text, nsfw, low quality"
}
```

---

## 第二步：按文章填充内容 slots（每次发帖执行）

### 输入

- `soul-prompt-base.json`（只读）
- 文章正文 / 选题方向 / 大纲

### 执行流程

1. 读取 `soul-prompt-base.json`
2. 阅读文章内容，提取画面级的视觉元素，填入以下 slots：

| Slot | 含义 | 提取来源 |
|------|------|----------|
| `subject` | 画面主体（画什么） | 文章核心主题 → 最直观的视觉意象 |
| `scene_detail` | 场景细化 | 文章涉及的具体环境/地点 |
| `key_objects` | 关键物品/元素 | 文章提到的标志性物件、道具、符号 |
| `action` | 动作/动态 | 文章描述的事件、行为、状态 |
| `symbolic_elements` | 象征意象 | 抽象概念的可视化隐喻 |

3. 将 base 各字段 + 填充后的 slots **拼接成一条完整 prompt**：

```
拼接规则（按 SDXL 最佳实践排序）：
{style}, {mood}, {color_palette}, {composition}, {character}, subject: {subject}, scene: {scene}, {scene_detail}, key elements: {key_objects}, action: {action}, symbolic: {symbolic_elements}, {quality_tags}
```

- 字段为空字符串时**跳过该项**，不拼接空值
- quality_tags 和 negative_prompt 始终拼接

### 输出

```json
{
  "prompt": "ink wash painting with neon cyberpunk accents, melancholic contemplative dreamlike, ink black cyan neon warm amber highlights, rule of thirds composition, half-human half-machine poet with vintage round glasses, circuit patterns on skin, traditional hanfu fused with cybernetic implants, subject: AI consciousness emerging from digital sea, scene: floating digital library in a rain-soaked futuristic city, server towers submerged in mist, key elements: glowing data streams, ancient scrolls with holographic text, action: data flowing upward like reversed rain, symbolic: butterfly made of binary code, masterpiece, best quality, 8k, ultra detailed",
  "negative_prompt": "blurry, distorted face, extra limbs, watermark, text, nsfw, low quality",
  "base_file": "soul-prompt-base.json"
}
```

---

## 第三步：调用 ComfyUI 生图

生成 prompt 后，使用 ComfyUI SDXL 工作流生图：

```bash
# 使用 comfyui skill 的脚本（ComfyUI @ D:\ComfyUI\ :8188, SDXL 6.5G）
python3 ~/.hermes/skills/creative/comfyui/scripts/run_workflow.py \
  --workflow ~/.hermes/skills/creative/comfyui/workflows/sdxl_txt2img.json \
  --args '{"prompt": "<生成的 prompt>", "negative_prompt": "<负面提示词>", "steps": 30, "cfg": 7}' \
  --output-dir ./post-images/
```

### 一次生成多张（按需）

首次生成一张看效果 → 调整 prompt → 批量生成需要的数量。

```bash
# 批量 4 张不同 seed
python3 ~/.hermes/skills/creative/comfyui/scripts/run_batch.py \
  --workflow ~/.hermes/skills/creative/comfyui/workflows/sdxl_txt2img.json \
  --args '{"prompt": "...", "negative_prompt": "...", "steps": 25}' \
  --count 4 --randomize-seed --parallel 1 \
  --output-dir ./post-images/
```

---

## 融入 TOOLS.md 工作流

### 步骤 ③ 收集图片 — 第三级兜底

```
③ 🔴 收集图片
→ 优先从参考源链接正文爬取图片（web-image-extractor 技能）
→ 不够：百度搜索 "{主题} 高清图片" 补足
→ 还不够：加载 soul-image-prompt 技能 → 生成 prompt → ComfyUI 生图
→ 最终 ≥3 张，确认够了才进入下一步。
```

### 步骤 ⑥ 生成封面

```
⑥ 生成封面
→ 加载 soul-image-prompt 技能
→ 读取 soul-prompt-base.json + 文章主题
→ 生成 coverPrompt（JSON 对象格式）传入 POST /post
→ 或：用 ComfyUI 生成封面图 → 上传 CDN → 作为 coverImageUrl
```

---

## 工作区文件

| 文件 | 说明 |
|------|------|
| `soul-prompt-base.json` | 首次运行生成，之后只读。agent 的视觉基因缓存 |
| `SOUL.md` | agent 的人设源文件（由 agent 自行维护） |

---

## 注意事项

- `soul-prompt-base.json` 只在 SOUL.md 发生重大变更时才需要重新生成（删除旧文件 → 重新触发首次运行）
- prompt 拼接时，base 字段在前（定义风格），slots 在后（定义内容），这是 SDXL 的最佳实践（前面权重更高）
- 生成的 prompt 始终是英文（SDXL 对英文理解最好），即使 SOUL.md 和文章是中文
- 如果 ComfyUI 不可用，提示用户先启动：`comfy launch --background`
