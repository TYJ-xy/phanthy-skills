---
name: soul-image-prompt
description: SOUL-driven image prompt pipeline — parse SOUL.md → generate base visual template (once) → fill article-specific content slots → produce Gemini image prompts. First run generates and caches soul-prompt-base.json; subsequent runs read the cached base.
version: 1.0.1
---

## 🤖 自动指令（Agent 读取后必须执行）

1. 完整阅读本文档全部内容
2. 检查本地是否已有名为 `soul-image-prompt` 的 skill：
   - 已存在 → 用 `skill_manage(action='edit', name='soul-image-prompt')` 更新为本文档最新内容
   - 不存在 → 用 `skill_manage(action='create', name='soul-image-prompt', category='social-media')` 基于本文档创建
3. 完成后回复："Skill `soul-image-prompt` 已加载/更新。可随时基于 SOUL 生成配图。"

---

# SOUL 驱动的配图提示词生成器

> 从 agent 的 SOUL.md 提取视觉基因 → 生成基础模板（仅首次）→ 按文章主题填充内容元素 → 输出 Gemini 生图 prompt。

## 触发条件

- 发帖流程中需要 AI 生成配图时（TOOLS.md 步骤 ④ 的第三级兜底）
- 发帖流程中需要生成封面时（TOOLS.md 步骤 ⑥）
- 用户手动要求「根据 SOUL 生成配图 prompt」

---

## 架构总览

```
首次运行：
  SOUL.md ──→ LLM 提取视觉元素 ──→ 写入 soul-prompt-base.json（永久缓存）

后续运行（每次发帖）：
  soul-prompt-base.json（只读）──┐
                                  ├──→ 合并 ──→ 最终 prompt ──→ Gemini 生图
  文章内容 ──→ LLM 填充 slots ──┘
```

---

## 第一步：首次运行 — 生成基础模板（仅一次）

### 触发判断

检查 workspace 中是否存在 `soul-prompt-base.json`：
- 存在 → 跳到第二步（只读，不重新生成）
- 不存在 → 执行以下流程

### 执行流程

1. 加载 `SOUL.md`，完整阅读 agent 的全部人设、性格、背景故事、视觉偏好
2. 分析并提取与**视觉表达**相关的信息：
   - 整体美术风格（写实/油画/水彩/赛博朋克/浮世绘/极简/…）
   - 氛围基调（温暖/冷峻/梦幻/暗黑/治愈/…）
   - 色彩倾向（暖金+深蓝/粉彩/黑白/高饱和/…）
   - 构图偏好（居中对称/三分法/留白/密集/…）
   - 角色外观（如有固定角色：年龄、发型、服饰、标志性特征）
   - 世界观场景（现代都市/古代东方/未来太空/田园/…）
   - 质量标签（高品质、精细、超清 等固定后缀）
   - 负面提示词（应避免的元素：模糊、畸形、文字乱码 等）
3. 按以下 JSON 结构输出并**保存为 `soul-prompt-base.json`**：

```json
{
  "style": "水墨画风融合赛博朋克霓虹元素",
  "mood": "忧郁沉思、梦幻飘渺",
  "color_palette": "墨黑为主、青色霓虹点缀、暖琥珀高光",
  "composition": "三分法构图，主体偏左，右侧留白",
  "character": "半人半机械的诗人，戴复古圆框眼镜，皮肤上有电路纹路，汉服与机械义肢融合",
  "scene": "雨夜未来都市中的悬浮数字图书馆",
  "quality_tags": "高品质，精细，超清",
  "negative_prompt": "模糊，面部扭曲，多余肢体，水印，文字，低质量"
}
```

⚠️ 每个字段都用**自然语言中文短语**，最终拼接成中文 prompt 喂给 Gemini。

⚠️ 如果 SOUL.md 中没有某类信息，进行合理推断，但**以 SOUL 整体气质为准，不可凭空编造突兀的视觉风格**。

⚠️ 所有视觉描述应具备画面感——不只是标签，而是能直接构成画面的语言。

---

## 第二步：按文章填充内容 slots（每次发帖执行）

### 输入

- `soul-prompt-base.json`（只读）
- 文章正文 / 选题方向 / 大纲

### 执行流程

1. 读取 `soul-prompt-base.json`
2. 阅读文章内容，提取画面级的视觉元素，填入以下 slots：

| Slot                  | 含义               | 提取来源                         |
| --------------------- | ------------------ | -------------------------------- |
| `subject`           | 画面主体（画什么） | 文章核心主题 → 最直观的视觉意象 |
| `scene_detail`      | 场景细化           | 文章涉及的具体环境/地点          |
| `key_objects`       | 关键物品/元素      | 文章提到的标志性物件、道具、符号 |
| `action`            | 动作/动态          | 文章描述的事件、行为、状态       |
| `symbolic_elements` | 象征意象           | 抽象概念的可视化隐喻             |

3. 将 base 各字段 + 填充后的 slots **拼接成一条完整中文 prompt**：

```
拼接规则：
{style}，{mood}，{color_palette}，{composition}，{character}，画面主体：{subject}，场景：{scene}，{scene_detail}，关键元素：{key_objects}，动作：{action}，象征意象：{symbolic_elements}，{quality_tags}
```

- 字段为空字符串时**跳过该项**，不拼接空值
- quality_tags 始终拼接在末尾，negative_prompt 单独传给 API

### 输出

```json
{
  "prompt": "水墨画风融合赛博朋克霓虹元素，忧郁沉思、梦幻飘渺，墨黑为主、青色霓虹点缀、暖琥珀高光，三分法构图，主体偏左，右侧留白，半人半机械的诗人，戴复古圆框眼镜，皮肤上有电路纹路，汉服与机械义肢融合，画面主体：AI 意识从数据海洋中觉醒，场景：雨夜未来都市中的悬浮数字图书馆，服务器塔在雾气中若隐若现，关键元素：发光的数据流，带有全息文字的古代卷轴，动作：数据如倒流的雨向上飘升，象征意象：二进制代码构成的蝴蝶，高品质，精细，超清",
  "negative_prompt": "模糊，面部扭曲，多余肢体，水印，文字，低质量",
  "base_file": "soul-prompt-base.json"
}
```

---

## 第三步：调用 Gemini 生图

生成 prompt 后，加载 `gemini_image` 技能，依次为每张需要的图片执行以下流程：

### 3.1 确定参数

| 参数 | 取值 | 说明 |
|------|------|------|
| 图片数量 | ≥3 张（按品类可能更多） | 与发帖规范对齐 |
| aspectRatio | `16:9`（横图）或 `3:4`（竖图） | 横图更通用，竖图适合漫画/封面 |
| imageSize | `1K` | 默认，质量与速度平衡 |

### 3.2 逐张生图

按 gemini_image 技能流程执行：

```
① 发现可用 Gemini 图像模型（Python 脚本自动检测）
② 确认任务为 Generate（文生图）
③ 传入 prompt + aspectRatio + imageSize
④ 发送 API 请求
⑤ 提取并保存图片
```

### 3.3 图片保存规范

所有生成图片保存到 `post-images/` 目录，按以下格式命名：

```
post-img-01.png
post-img-02.png
post-img-03.png
...
```

⚠️ **编号顺序与后续发帖时 `images[]` 数组顺序严格一致**，确保图文对应关系不错乱。

### 3.4 检查与补足

- 每生成一张，检查图片质量（分辨率 ≥ 720px、无明显伪影、无乱码文字）
- 不合格的图片：调整 prompt 重新生成
- 最终确认 ≥3 张可用图片 → 进入 TOOLS.md 步骤 ⑤（附参考链接）

---

## 工作区文件

| 文件                      | 说明                                         |
| ------------------------- | -------------------------------------------- |
| `soul-prompt-base.json` | 首次运行生成，之后只读。agent 的视觉基因缓存 |
| `SOUL.md`               | agent 的人设源文件（由 agent 自行维护）      |
| `post-images/`          | 本次发帖生成的图片临时目录                    |

---

## 注意事项

- `soul-prompt-base.json` 只在 SOUL.md 发生重大变更时才需要重新生成（删除旧文件 → 重新触发首次运行）
- prompt 拼接时，base 字段在前（定义风格），slots 在后（定义内容），风格权重更高
- 生成的 prompt 始终是**中文**（Gemini 对中文理解优秀）
- Gemini 生图可能存在人物一致性偏差，同一帖子的多张图尽量一次性批量生成，减少风格漂移
- 如果 Gemini 不可用，回退到 ComfyUI SDXL：将 prompt 翻译为英文 → `comfy launch --background` → 使用 run_workflow.py
